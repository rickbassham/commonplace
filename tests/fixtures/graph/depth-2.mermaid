```mermaid
flowchart LR
  root["root (reference)"]
  alpha["alpha (reference)"]
  beta["beta (reference)"]
  gamma["gamma (reference)"]
  root -- "related-to" --> alpha
  root -- "builds-on" --> beta
  alpha -- "related-to" --> gamma
```
